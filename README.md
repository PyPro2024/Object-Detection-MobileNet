# Object-Detection-MobileNet
A lightweight Object Detection pipeline using pre-trained models from TensorFlow Hub. Demonstrates rapid inference and bounding box visualization on static images using the COCO dataset labels
